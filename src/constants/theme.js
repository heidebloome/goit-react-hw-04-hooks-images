export const theme = Object.freeze({
  colors: {
    white: "#ffffff",
    backgroundColor: "#add8e6",
    textColor: "#666565",
    iconColor: "#808080",
  },
  spacing: (value) => `${4 * value}px`,
});
